{
  "name": "lior-playerlist",
  "version": "1.0.0",
  "description": "LIOR_PLAYERLIST FiveM player monitor (server-side proxy, no CORS issues)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
